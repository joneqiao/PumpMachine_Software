电机打水那个机子要加一个功能，长按此键3s，
将此时的触力传感器的压力校准为0mmHg，原来设置高度的那个页面用一个单独的密码从主界面直接进入，
密码设置为1234


2023年8月30日 升级需求
1、负压数值显示区域增加了按键，按下弹出键盘，可预设负压值
    --初始化的时候，从flash中先读出预设的值
    --确定设置的上下限 0~600
    --从屏幕得到设定的值(返回值的地址为0x1119)
    --往flash里存储的para2flash[parameternum] --> parameternum = 27 --> 吸引泵的设定压力值

2、在负压泵不开启时，显示预设负压值，负压泵开启时，显示实际负压值